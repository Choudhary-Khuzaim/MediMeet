import 'package:flutter/material.dart';

class AppColors {
  // Three-color professional palette
  static const Color primary = Color(0xFF1D3557); // Deep Navy
  static const Color secondary = Color(0xFF00A0AB); // Teal
  static const Color accent = Color(0xFFFFB703); // Warm Amber

  // Neutrals
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFEEF1F5);

  // Text
  static const Color textPrimary = Color(0xFF1B1F3B);
  static const Color textSecondary = Color(0xFF5C6272);
  static const Color textMuted = Color(0xFF99A0AD);

  // Status
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFEF6C00);
  static const Color error = Color(0xFFD32F2F);
  static const Color info = Color(0xFF1E88E5);

  // Borders & dividers
  static const Color border = Color(0xFFE0E5EC);
  static const Color divider = Color(0xFFE6E9EF);

  // Shadows
  static Color shadowColor = Colors.black.withOpacity(0.06);
  static Color shadowColorDark = Colors.black.withOpacity(0.12);
}

